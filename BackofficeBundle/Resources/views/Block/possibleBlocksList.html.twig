{% for block in blocks %}
    <li class="block newly-inserted">
        <div class="panel-block generate bg-orchestra-orange" data-block-type="{{ block }}">
            <span class="title"></span>
            <div class="preview">
                {{ display_icon(block) }}
            </div>
        </div>
    </li>
{% endfor %}
